const sum = (x, y) => {
  return x + y;
}

// === === === === ===

const square = function(x) {
  return x * x;
}

const square = (x) => {
  return x * x;
}

// === === === === ===

const isEven = function (num) {
  return num % 2 === 0;
}

const isEven = (num) => {
  return num % 2 === 0;
}

const isEven = num => {
  return num % 2 === 0;
}

const isEven = num => (
  num % 2 === 0
)

const isEven = num => num % 2 === 0;

// === === === === ===

const multiply = (x, y) => {
  return x * y;
}

// === === === === ===

